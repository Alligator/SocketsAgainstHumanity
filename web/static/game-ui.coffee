class GameAPI
  constructor: ->

  registrationResponse: (success, reason) ->

  gameList: ->
  gameCreateResponse: ->
  gameJoinResponse: ->

  gameState: ->
  playerList: ->
  roundData: ->
  handList: ->
  playedCards: ->
  winnerSelected: ->
  chatMessage: ->

  submitCardResponse: ->
